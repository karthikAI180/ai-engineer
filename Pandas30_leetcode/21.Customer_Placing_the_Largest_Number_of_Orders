import pandas as pd

def largest_orders(orders: pd.DataFrame) -> pd.DataFrame:
    k=orders.groupby('customer_number')['order_number'].count().reset_index(name='cnt')
    filt=k['cnt']==(k['cnt'].max())
    return k.loc[filt,['customer_number']]
    